[√]set up files
    [√]JQuery
    [√]server.js
    [√]client.js
    [√]main.css
    [√]HTML with all linked in
    [√]verify all linked in properly
[√]set up/install for server
    [√]npm init
    [√]express install
    [√]gitignore folder w/node_modules inside
    [√]install body parsers  
[]set up frame work HTML
    [√]user has two input fields for values
    [√]user selects operator
    [√]submit button when clicked captures input and bundles it in object
    [√]send object to the server via post
    []clear button that clears the user input fields
[√]server side logic
    [√]use the object sent from client side to do the needed math
    [√]send back an ok
    [√]use GET request after the post to get the calculation
[√]History
    [√]store the history in an array on server side
    [√]display a list of all previous calculations of page loads
    using a get request.
    [√]list updates upon new calculation
[√]math
    [√]create function that takes in inputs and dose the needed math
    [√]change result var to reflect current resutls
    [√] append results to end of history equation 